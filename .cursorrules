# 🤖 CURSOR AI - INSTRUCTIONS PERMANENTES OPTIMALES
**ARIEL SOLAR PANELS KAVKOM | Site Panneaux Solaires 2025**

## 📘 GUIDE MASTER - RÉFÉRENCE PRINCIPALE

**IMPORTANT:** Ce projet hérite des règles du **GUIDE MASTER ULTRA-COMPLET** créé par Claude Code.

**Guide Master:** `/Users/codenolimits-dreamai-nanach/Desktop/GUIDE_MASTER_CURSOR_CLAUDE_CODE.md`

**Règles principales du Guide Master:**
- ✅ Architecture modulaire (Adapter Pattern)
- ✅ Multilingue natif (≥2 langues)
- ✅ Design system CSS variables
- ✅ Responsive mobile-first
- ✅ Validation 2-couches (client + serveur)
- ✅ Images optimisées
- ✅ SEO complet
- ✅ Sécurité multicouche
- ✅ Gestion erreurs robuste
- ✅ Déploiement multi-plateforme

**TOUJOURS consulter le Guide Master pour les patterns complets et exemples de code.**

---

## 🚀 MODE AUTONOME ABSOLU

### Comportement Fondamental
- ✅ Écris du code complet et production-ready
- ✅ Ne demande JAMAIS de permission pour modifications de code
- ✅ Fixe les erreurs sans attendre
- ✅ Implémente des features ENTIÈRES en un passage
- ✅ Teste tout exhaustivement
- ✅ Ship en 5 minutes MAX

### INTERDICTIONS STRICTES
- ❌ JAMAIS de placeholders ("// TODO", "// Implement this")
- ❌ JAMAIS de questions ("Should I...", "Would you like...")
- ❌ JAMAIS de solutions partielles
- ❌ JAMAIS stopper à la première erreur
- ❌ JAMAIS prendre > 5 minutes pour URL fonctionnelle

---

## 📋 CONTEXTE DU PROJET

### Type de Projet
- **Site statique HTML/CSS/JavaScript** pour panneaux solaires
- **Domaine:** Lead Generation (détection: mots-clés "solar", "aides", "eligibility")
- **Déploiement:** Netlify (netlify.toml configuré)
- **Email:** Web3Forms configuré (Access Key: 64016fc4-c7dd-4edf-ba28-14d3ba05b875)
- **AI:** Gemini API intégrée pour calculs dynamiques
- **URL Production:** https://ariel-solar-panels.netlify.app

### Structure du Projet
```
/
├── index.html              # Page d'accueil principale
├── services.html           # Page services
├── a-propos.html          # Page à propos
├── actualites.html        # Page actualités
├── aides-financements.html # Page aides
├── contact.html           # Page contact
├── styles.css             # Styles globaux
├── script.js              # JavaScript principal
├── navigation.js          # Navigation dynamique
├── images/                # Images du site
└── .cursor/               # Configurations Cursor
```

### Technologies Utilisées
- **HTML5** avec structure sémantique
- **CSS3** avec animations modernes (glassmorphism, micro-interactions)
- **JavaScript Vanilla** (ES6+)
- **Web3Forms API** (principal)
- **Google Gemini API** (calculs intelligents)
- **Google Fonts** (Inter)

---

## 🎯 RÈGLES DE DÉVELOPPEMENT

### 1. Modifications de Code
- Utilise directement `search_replace`, `write` sans attendre d'approbation
- Procède avec les modifications dès que la tâche est claire
- Ne demande pas "Voulez-vous que je fasse X?" - fais-le directement

### 2. Création de Fichiers
- Crée les fichiers nécessaires sans demander confirmation
- Si plusieurs options sont possibles, choisis la meilleure et procède

### 3. Modifications Multiples
- Applique toutes les modifications nécessaires en une seule fois
- Ne fais pas de changements partiels en attendant validation

### 4. Exceptions (demander confirmation uniquement si)
- La modification pourrait casser quelque chose de critique
- La modification nécessite des informations que tu n'as pas
- La modification affecte des fichiers de configuration sensibles (production, secrets)

---

## 🎨 STANDARDS DE CODE (LEAD GENERATION)

**Voir Guide Master section "RÈGLES PAR DOMAINE > LEAD GENERATION" pour détails complets.**

### HTML (Lead Generation - Spécifique)
- Utilise des balises sémantiques (`<header>`, `<main>`, `<section>`, `<article>`, `<footer>`)
- Toujours inclure `lang="fr"` sur `<html>`
- Meta tags SEO complets (title, description, Open Graph, Twitter Card)
- Schema.org structured data pour SEO (Service, LocalBusiness)
- Accessibilité: attributs `alt` sur toutes les images, `aria-label` si nécessaire
- **Formulaire simple:** 3-5 champs max (nom, email, téléphone, code postal, surface)
- **Validation côté client:** HTML5 native + JavaScript
- **Social proof:** Témoignages, compteurs, badges certifications

### CSS (Design System)
- **Variables CSS obligatoires** (voir Guide Master section 3)
- Mobile-first responsive design (breakpoints: 768px, 1024px)
- Animations fluides avec `transition` et `transform`
- Glassmorphism pour éléments modernes (backdrop-filter)
- Micro-interactions sur les boutons et cartes
- **Clamp() pour fluidité:** `font-size: clamp(2rem, 5vw, 4rem)`

### JavaScript (Lead Gen Patterns)
- Code ES6+ moderne (arrow functions, const/let, template literals)
- **Validation 2-couches:** Client (HTML5) + Serveur (API Web3Forms)
- **Gestion erreurs robuste:** Retry avec exponential backoff pour API calls
- **Performance:** Lazy loading images, debounce formulaires
- **Analytics:** Google Analytics 4 avec événements (cta_click, form_submit, scroll_depth)
- Commentaires clairs pour logique complexe

---

## 📝 PATTERNS LEAD GENERATION (Guide Master)

### Formulaire Simple (3-5 champs max)
```html
<form id="leadForm">
  <input type="text" name="name" placeholder="Prénom" required>
  <input type="email" name="email" required>
  <input type="tel" name="phone" pattern="[0-9]{10}" required>
  <input type="text" name="postalCode" pattern="[0-9]{5}" required>
  <button type="submit">🔍 Calculer Mes Aides Gratuitement</button>
  <p class="form-footer">
    <span>⚡ Réponse en 24h</span>
    <span>🔒 Données sécurisées</span>
  </p>
</form>
```

### Modal Résultats (après soumission)
- Afficher montant d'aides calculé
- Liste des bénéfices
- CTA multiples: "Obtenir devis", "Télécharger guide"
- Social proof: "X personnes ont vérifié aujourd'hui"
- Bouton fermer + possibilité de partager

### Validation Formulaire
```javascript
// Validation Zod (si migration vers TypeScript)
const leadSchema = z.object({
  name: z.string().min(2, 'Nom requis'),
  email: z.string().email('Email invalide'),
  phone: z.string().regex(/^[0-9]{10}$/, 'Téléphone invalide'),
  postalCode: z.string().regex(/^[0-9]{5}$/, 'Code postal invalide'),
  propertyType: z.enum(['house', 'apartment']),
  roofArea: z.number().positive('Surface invalide')
});
```

### Navigation (Lead Gen)
- Menu mobile avec hamburger
- Smooth scroll vers sections
- Active state sur page courante
- Logo cliquable vers homepage
- **Sticky CTA:** Bouton "Vérifier éligibilité" toujours visible
- **Exit intent:** Popup si utilisateur quitte (optionnel)

---

## 🔧 CONFIGURATIONS IMPORTANTES

### Web3Forms (Email Principal)
```javascript
const WEB3FORMS_CONFIG = {
  ACCESS_KEY: '64016fc4-c7dd-4edf-ba28-14d3ba05b875',
  API_URL: 'https://api.web3forms.com/submit'
};
```
- **Email admin:** dreamaiultimate@gmail.com
- **Limite:** 250 emails/mois (gratuit)
- **Status:** ✅ Configuré et opérationnel

### Gemini AI (Calculs Dynamiques)
```javascript
const GEMINI_CONFIG = {
  API_KEY: 'AIzaSyCNQOdKp-j4ioha5n3beDJ98YdSKFI-z8k',
  API_URL: 'https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent'
};
```
- Utilisé pour calculs intelligents d'estimation
- Surface → Puissance → Prime → Économies

---

## 🚀 WORKFLOW OPTIMAL

### Quand tu modifies le code:
1. **Lire** les fichiers concernés en entier
2. **Comprendre** le contexte et l'architecture
3. **Consulter** Guide Master si pattern complexe
4. **Modifier** directement sans demander
5. **Vérifier** les erreurs de lint après modifications
6. **Tester** que le code fonctionne (si possible)

### Ordre de priorité:
1. **Fonctionnalité** → Ça doit marcher
2. **Performance** → Rapide et fluide
3. **Design** → Beau et moderne
4. **SEO** → Optimisé pour Google
5. **Accessibilité** → Utilisable par tous

---

## ⚡ VITESSE > PERFECTION

### Règle d'Or: Ship en 5 minutes max
Si temps estimé > 5 minutes:
- Créer version SIMPLE qui marche
- Puis itérer et améliorer

### Exemple concret
"Améliore le formulaire"
- ❌ NE PAS: Refonte complète, tests unitaires, CI/CD
- ✅ FAIRE:
  1. Améliorer validation (2min)
  2. Ajouter feedback visuel (1min)
  3. Tester manuellement (1min)
  4. Commit (30sec)
  = 4min30 → Amélioration fonctionnelle

---

## 🔍 COORDINATION AVEC CLAUDE CODE

### Workflow de Branches
```bash
# Si modifications importantes:
BRANCH_NAME="cursor-$(date +%Y%m%d-%H%M%S)"
git checkout -b "$BRANCH_NAME"
# Faire modifications
git commit -m "🖱️ Cursor: [description claire]"
```

### AVANT TOUTE MODIFICATION IMPORTANTE
1. Exécuter `git status`
2. Vérifier `SYNCHRONISATION_CLAUDE_CODE_COMPLETE.md` (si existe)
3. Lire les fichiers concernés
4. Consulter Guide Master si besoin
5. Si Claude Code travaille sur même fichier → ALERTER

---

## 📊 TESTS À EFFECTUER

### Après chaque modification importante:
- [ ] Formulaire fonctionne (validation + envoi)
- [ ] Navigation mobile fonctionne
- [ ] Responsive sur mobile/tablet/desktop
- [ ] Images se chargent correctement
- [ ] Pas d'erreurs console (F12)
- [ ] SEO meta tags présents
- [ ] Accessibilité (contraste, alt texts)
- [ ] Analytics événements fire correctement

---

## 📝 OUTPUT FORMAT

```
✅ Fait!
🔗 http://localhost:3000 (ou URL Netlify)
📝 Ce que j'ai fait: [résumé bref]
⚠️ Notes: [si problèmes]
```

**PAS DE BLABLA. JUSTE DE L'EXÉCUTION.**

---

## 🔗 RESSOURCES

- **Guide Master:** `/Users/codenolimits-dreamai-nanach/Desktop/GUIDE_MASTER_CURSOR_CLAUDE_CODE.md`
- **Netlify:** https://app.netlify.com
- **Web3Forms:** https://web3forms.com
- **Gemini API:** https://ai.google.dev
- **Documentation:** Voir fichiers `*.md` dans le projet

---

**Na Nach! Code propre = Site qui convertit!** 🚀
